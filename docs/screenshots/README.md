# Screenshots

Drop the README's referenced screenshot files here (1–2 images is most
professional). After capturing, uncomment the image lines in the main README.

Capture tips:
- Use a clean, real-looking but non-personal example state.
- Crop to the app; trim browser chrome and desktop background.
- Keep them reasonably sized (PNG, ~1200px wide is plenty).
